<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Book Concert Tickets</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
</head>
<body class="bg-light">
    <div class="container mt-5">
        <h1 class="text-center">Book Tickets for <%= concert.concertName %></h1>

        <div class="card p-4 shadow mt-4">
            <p><strong>Date & Time:</strong> <%= new Date(concert.dateTime).toLocaleString() %></p>
            <p><strong>Venue:</strong> <%= concert.venue %></p>
            <p><strong>Ticket Price:</strong> ₹<%= concert.ticketPrice %></p>
            <p><strong>Available Tickets:</strong> <%= concert.availableTickets %></p>

            <% if (concert.availableTickets > 0) { %>
                <form action="/booking/book/<%= concert._id %>" method="POST">

                    <input type="hidden" name="concertId" value="<%= concert._id %>">
                

                    <h3 class="text-success text-center">Welcome, <%= userName || 'Guest' %>!</h3>
                    <input type="hidden" name="username" value="<%= userName || '' %>">


                    <div class="mb-3">
                        <label for="tickets" class="form-label">Number of Tickets (1-3):</label>
                        <input type="number" name="tickets" class="form-control" min="1" max="3" required>
                    </div>

                    <button type="submit" class="btn btn-primary w-100">Book Now</button>
                   
                </form>
            <% } else { %>
                <div class="alert alert-danger text-center">Sold Out! No tickets available.</div>
            <% } %>
        </div>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
