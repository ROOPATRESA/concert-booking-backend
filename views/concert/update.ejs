<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>Update Concert</title>
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #74ebd5, #acb6e5);
            display: flex;
            justify-content: center;
            align-items: flex-start;
            padding-top: 50px;
            min-height: 100vh;
        }

        .form-container {
            background: #fff;
            padding: 40px 50px;
            border-radius: 15px;
            box-shadow: 0 8px 25px rgba(0, 0, 0, 0.1);
            width: 100%;
            max-width: 500px;
            transition: 0.3s ease;
        }

        .form-container:hover {
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.15);
        }

        h1 {
            text-align: center;
            color: #444;
            margin-bottom: 25px;
            font-weight: bold;
        }

        form label {
            font-weight: 600;
            display: block;
            margin-top: 20px;
            margin-bottom: 8px;
            color: #555;
        }

        form input[type="text"],
        form input[type="number"],
        form input[type="datetime-local"] {
            width: 100%;
            padding: 12px;
            border: 1px solid #ccc;
            border-radius: 8px;
            font-size: 15px;
            transition: all 0.2s;
        }

        form input[type="text"]:focus,
        form input[type="number"]:focus,
        form input[type="datetime-local"]:focus {
            border-color: #2196F3;
            box-shadow: 0 0 5px rgba(33, 150, 243, 0.3);
            outline: none;
        }

        form input[type="file"] {
            margin-top: 8px;
        }

        form input[type="submit"] {
            margin-top: 25px;
            width: 100%;
            padding: 14px;
            background: #2196F3;
            color: white;
            border: none;
            font-size: 16px;
            border-radius: 8px;
            cursor: pointer;
            transition: background 0.3s;
        }

        form input[type="submit"]:hover {
            background: #1976D2;
        }

        .error-box {
            background: #ffebee;
            border: 1px solid #f44336;
            color: #d32f2f;
            padding: 15px 20px;
            border-radius: 8px;
            margin-bottom: 20px;
        }

        .error-box ul {
            margin: 5px 0 0 20px;
            padding: 0;
        }

        .preview-image {
            width: 100%;
            height: 220px;
            object-fit: cover;
            margin-top: 15px;
            border-radius: 8px;
            box-shadow: 0 0 8px rgba(0, 0, 0, 0.1);
        }
    </style>
</head>

<body>

    <div class="form-container">
        <h1>Update Concert</h1>

        <% if (error) { %>
            <div class="error-box">
                <p>Error(s):</p>
                <ul>
                    <% Object.values(error).forEach(err => { %>
                        <li><%= err.message %></li>
                    <% }); %>
                </ul>
            </div>
        <% } %>

        <form action="/concert/update_concert/<%= concert._id %>" method="post" enctype="multipart/form-data" >

            <label for="concertName">Concert Name:</label>
            <input type="text" id="concertName" name="concertName" value="<%= concert.concertName %>" required>

            <label for="dateTime">Date and Time:</label>
            <input type="text" id="dateTime" name="dateTime" value="<%= concert.dateTime %>" required>

            <label for="venue">Venue:</label>
            <input type="text" id="venue" name="venue" value="<%= concert.venue %>" required>

            <label for="ticketPrice">Ticket Price:</label>
            <input type="number" id="ticketPrice" name="ticketPrice" step="0.01" value="<%= concert.ticketPrice %>" required>

            <label for="availableTickets">Available Tickets:</label>
            <input type="number" id="availableTickets" name="availableTickets" value="<%= concert.availableTickets %>" required>

            <label for="image">Concert Image:</label>
            <input type="file" name="image" accept="image/*">

            <% if (concert.imageURL) { %>
                <div>
                    <img class="preview-image" src="<%= concert.imageURL %>" alt="Concert Image">
                </div>
            <% } %>
            <input type="submit" value="Update Concert">

        </form>
    </div>

</body>

</html>
