<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Live Band Concerts</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }

        /* Navbar */
        .navbar {
            background: linear-gradient(45deg, #000000, #1a1a1a);
        }

        .navbar-brand {
            font-size: 1.8rem;
            font-weight: bold;
        }

        /* Carousel */
        .carousel-item {
            height: 100vh;
            background-size: cover;
            background-position: center;
            position: relative;
        }

        .carousel-caption {
            background: rgba(0, 0, 0, 0.6);
            padding: 30px;
            border-radius: 15px;
            animation: fadeInUp 1s;
        }

        .carousel-caption h1 {
            font-size: 3rem;
            font-weight: 700;
        }

        .carousel-caption p {
            font-size: 1.2rem;
            margin-bottom: 15px;
        }

        .carousel-caption .btn {
            border-radius: 50px;
            padding: 10px 25px;
            font-weight: bold;
        }

        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        /* Carousel Controls */
        .carousel-control-prev-icon,
        .carousel-control-next-icon {
            background-color: rgba(0, 0, 0, 0.7);
            border-radius: 50%;
            padding: 10px;
        }

        /* Footer */
        footer {
            background: #111;
            font-size: 0.9rem;
            letter-spacing: 1px;
        }
    </style>
</head>

<body>

    <!-- Navbar -->
    <nav class="navbar navbar-dark fixed-top">
        <div class="container">
            <a class="navbar-brand" href="/">🎸 Band Concerts</a>
            <div>
                <% if (userEmail) { %>
                    <a class="btn btn-outline-light" href="/logout">Logout</a>
                <% } else { %>
                    <a class="btn btn-primary me-2" href="/signup">Sign Up</a>
                    <a class="btn btn-outline-light" href="/login">Login</a>
                <% } %>
            </div>
        </div>
    </nav>

    <!-- Carousel -->
    <div id="concertCarousel" class="carousel slide" data-bs-ride="carousel">

        <div class="carousel-inner">

            <div class="carousel-item active" style="background-image: url('https://images.pexels.com/photos/1763067/pexels-photo-1763067.jpeg?auto=compress&cs=tinysrgb&w=600');">
                <div class="carousel-caption">
                    <h1>Welcome to Live Band Concerts</h1>
                    <p>Feel the energy, live the moment.</p>
                    <a href="/signup" class="btn btn-primary btn-lg">Get Started - Sign Up</a>
                </div>
            </div>

            <div class="carousel-item" style="background-image: url('https://images.pexels.com/photos/196652/pexels-photo-196652.jpeg?auto=compress&cs=tinysrgb&w=600');">
                <div class="carousel-caption">
                    <h1>Book Your Favorite Band Shows</h1>
                    <p>Exciting performances await you.</p>
                    <a href="/signup" class="btn btn-success btn-lg">Join Now</a>
                </div>
            </div>

            <div class="carousel-item" style="background-image: url('https://images.pexels.com/photos/1120162/pexels-photo-1120162.jpeg?auto=compress&cs=tinysrgb&w=600');">
                <div class="carousel-caption">
                    <h1>Enjoy Music Like Never Before</h1>
                    <p>Secure your tickets now!</p>
                    <a href="/signup" class="btn btn-warning btn-lg text-dark">Sign Up Free</a>
                </div>
            </div>

        </div>

        <!-- Carousel Controls -->
        <button class="carousel-control-prev" type="button" data-bs-target="#concertCarousel" data-bs-slide="prev">
            <span class="carousel-control-prev-icon"></span>
        </button>
        <button class="carousel-control-next" type="button" data-bs-target="#concertCarousel" data-bs-slide="next">
            <span class="carousel-control-next-icon"></span>
        </button>

    </div>

    <!-- Footer -->
    <footer class="text-center p-3 text-white mt-3">
        &copy; 2025 Band Concerts. All Rights Reserved.
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>
